import styled from "styled-components";

const Item = styled.li`
display: flex;
padding: 12px 18px;
align-items: center;
gap: 8px;
border-radius: 100px;
background: var(--input-white-color);
`;

export {Item}